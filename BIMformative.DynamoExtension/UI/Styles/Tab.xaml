<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/BIMformative.DynamoExtension;component/UI/Styles/Brushes.xaml" />
        <ResourceDictionary Source="/BIMformative.DynamoExtension;component/UI/Styles/Colors.xaml" />
        <ResourceDictionary Source="/BIMformative.DynamoExtension;component/UI/Styles/Fonts.xaml" />
    </ResourceDictionary.MergedDictionaries>
    
    <!--Tab Header Template-->
    <DataTemplate x:Key="TabHeaderTemplate">
        <TextBlock Text="{Binding Header}"
                   FontFamily="{StaticResource BIMFont}"
                   FontSize="13"
                   Foreground="#E5E5E5"
                   VerticalAlignment="Center"
                   TextTrimming="CharacterEllipsis"
                   Margin="10,0,0,0" />
    </DataTemplate>

    <!--TabItem Style-->
    <Style x:Key="ScriptManagerTabItemStyle" TargetType="TabItem">
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="FontSize" Value="13" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="Root"
                            Margin="0"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Background="Transparent"
                            BorderBrush="Transparent"
                            BorderThickness="0"
                            CornerRadius="6">
                        <StackPanel Margin="0,5,0,0"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Stretch"
                                    Orientation="Horizontal">
                            <Grid Name="TabPanel"
                                  Width="220"
                                  Height="30"
                                  Margin="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="5" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Label x:Name="SelectedItem" Grid.Column="0" />
                                <ContentPresenter x:Name="ContentSection" 
                                                  Grid.Column="1"
                                                  Margin="5,0,0,0"                                                  
                                                  HorizontalAlignment="Stretch"
                                                  VerticalAlignment="Center"
                                                  ContentSource="Header" />  
                            </Grid>
                        </StackPanel>                        
                    </Border>

                    <ControlTemplate.Triggers>                        
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="SelectedItem" Property="Background" Value="{StaticResource PrimaryBrush}" />
                            <Setter Property="Foreground" Value="White" />
                            <Setter Property="FontWeight" Value="Bold" />
                        </Trigger>

                        <Trigger Property="IsSelected" Value="False">
                            <Setter TargetName="SelectedItem" Property="Background" Value="Transparent" />
                            <Setter Property="Foreground" Value="White" />
                            <Setter Property="FontWeight" Value="Regular" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--TabControl Style-->
    <Style x:Key="ScriptManagerTabControlStyle" TargetType="TabControl">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="ItemContainerStyle" Value="{StaticResource ScriptManagerTabItemStyle}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <DockPanel LastChildFill="True">
                        <TabPanel x:Name="HeaderPanel"
                                  DockPanel.Dock="Left"
                                  IsItemsHost="True"
                                  Background="Transparent"                                  
                                  Margin="0" />
                        <ContentPresenter ContentSource="SelectedContent"
                                          Margin="6"                                          
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </DockPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>